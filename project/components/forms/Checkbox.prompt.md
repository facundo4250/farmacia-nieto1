Checkbox for opt-ins and multi-select lists (consent, newsletter, specialties).

```jsx
<Checkbox label="Quiero recibir novedades por email" defaultChecked />
<Checkbox label="Acepto la política de privacidad" />
```

Controlled (`checked`) or uncontrolled (`defaultChecked`). Custom forest-green check; supports `disabled`.
