Single-choice control. Group several with the same `name`.

```jsx
<Radio name="retiro" value="local" label="Retiro en el local" defaultChecked />
<Radio name="retiro" value="envio" label="Envío a domicilio" />
```

Custom forest dot; supports `disabled`.
