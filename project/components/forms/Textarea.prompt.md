Multi-line text field. Use for free-form notes — prescription details, a message to the pharmacist.

```jsx
<Textarea label="Contanos qué necesitás" rows={5} placeholder="Detalle de la receta o consulta…" />
```

Resizes vertically. Same `label` / `hint` / `error` / `required` API as Input.
