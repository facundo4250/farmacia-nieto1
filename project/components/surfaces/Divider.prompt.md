Section separator.

```jsx
<Divider />
<Divider label="Especialidades" />
<Divider mark />
<Divider orientation="vertical" />
```

Use `label` for a titled break, `mark` for a single honey dot, `orientation="vertical"` between inline items.
