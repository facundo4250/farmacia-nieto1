Chip for filters and selected items — pairs naturally with the eight specialties.

```jsx
<Tag dot>Homeopatía</Tag>
<Tag selected onClick={toggle}>Dermocosmética</Tag>
<Tag onRemove={remove}>Florales</Tag>
```

`selected` fills it forest-green; `onClick` makes it a toggle; `onRemove` adds an ×.
