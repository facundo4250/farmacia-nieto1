Small hover/focus bubble for hints and clarifications.

```jsx
<Tooltip content="Elaborado en 48–72 h"><IconButton label="Info"><InfoIcon/></IconButton></Tooltip>
```

`placement` is `top` (default) or `bottom`. Wraps any trigger; works on hover and keyboard focus.
